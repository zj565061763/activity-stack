<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name="com.sd.lib.actstack.LibContentProvider"
            android:authorities="${applicationId}.f-cp-lib-activity-stack"
            android:exported="false" />
    </application>

</manifest>
